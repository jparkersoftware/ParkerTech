{
  "name": "parkertech-vault-mcp",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "MCP server that exposes the ParkerTech Command Centre (Firestore) to Claude Code so it can read and write structured data alongside the Obsidian vault.",
  "main": "index.ts",
  "scripts": {
    "start": "tsx index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.0",
    "tsx": "^4.19.0",
    "zod": "^3.23.8"
  }
}
